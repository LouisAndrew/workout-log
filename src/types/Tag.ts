export type Tag = {
  /**
   * text to be rendered within the tag
   */
  text: string;
  /**
   * Category of the tag
   */
  category?: string
  /**
   * color for the rendered tag. default: #333
   */
  color?: string;
};
